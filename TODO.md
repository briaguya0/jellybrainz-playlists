# TODO

## Coverage improvements

Current baseline: **73.8% statements / 74.1% branches** (281 tests across 32 files).

### 1. Exclude generated/infrastructure files from coverage

Add to `vitest.config.ts` coverage `exclude`:

- `src/routeTree.gen.ts` — auto-generated by TanStack Router, never manually edited
- `src/router.tsx` — app wiring only, no testable logic
- `src/@tanstack-query/**` — third-party devtools bundle

This will bring reported coverage numbers up to reflect only files we actually own.

### 2. `src/lib/musicbrainz.ts` — 54% stmts, 38% branches

The two search functions and their stale-MBID fallback paths are completely untested, along with some error paths. Add to `tests/musicbrainz.test.ts`:

- `searchRecordingsByRelease`: initial search returns results → returns them immediately (no sleep); initial search empty → fetches release lookup → if `data.id === releaseMbid` returns `[]`; if new id differs → retries search with new id
- `searchRecordingsByArtist`: same pattern — initial hit returns early; stale MBID → lookup → retry
- `fetchCollectionRecordings`: throws when response is non-ok (currently only the happy path and pagination are tested)
- `createCollection`: throws when response is non-ok with non-404/405 status (the `throw err` re-throw in the catch block)

Note: the stale-MBID paths call `sleep(1100)` internally. Mock `globalThis.setTimeout` or use `vi.useFakeTimers()` + `vi.runAllTimersAsync()` to avoid real delays.

### 3. `src/pages/PlaylistsPage/components/playlist-viewer/TrackTableRow.tsx` — 25% stmts

The low coverage is because the current tests mock `MbBadge`, `UnresolvedCell`, and `RecordingInfo`, so the inline callback props (`onConfirm`, `onOverride`, `onClear`) are never exercised. Add integration-style tests that render `TrackTableRow` without mocking its children:

- `partial-auto` state: open the MbBadge popover → click Confirm → `onSetOverride` called with track.Id and recording.id
- `partial-auto` state: open the MbBadge → click Change → enter MBID → Apply → `onSetOverride` called
- `partial-auto` state: open the MbBadge → click Change → Clear → `onClearOverride` called
- `override` state with recording: same Change/Clear paths
- `unresolved` state: click candidate → `onSetOverride` called; manual MBID input → `onSetOverride` called

These tests should mock `ThumbnailTooltip` (network image) but use the real `MbBadge`/`UnresolvedCell`.

### 4. `src/components/settings/MbSection.tsx` — 28% stmts

The clientId input and secret input forms are untested (lines 13–23 and 40–52). Add to `tests/components/settings/MbSection.test.tsx`:

- shows clientId input and secret input when no clientId is saved
- typing in the clientId field and saving calls `setClientId`
- typing in the secret field and saving calls `setClientSecret`
- shows "Clear" button for secret when secret is already saved; clicking it calls `clearClientSecret`

### 5. `src/pages/PlaylistsPage/components/playlist-viewer/SyncDropdown.tsx` — 55% stmts

The replace flow and some idle-state rendering paths are untested. Add to `tests/components/playlist-viewer/SyncDropdown.test.tsx`:

- replace flow: pick collection → click Replace → fetches `fetchCollectionRecordings` → shows replace-confirm with backup button; click backup → marks downloaded; click Replace → calls `deleteRecordingsFromCollection` then `addRecordingsToCollection` → done state
- error in replace: `fetchCollectionRecordings` throws → shows error state
- idle state: shows "no recording collections" message when `collections` is empty array
- idle state: Refresh button sets `collections` back to `null`, triggering a re-fetch
- idle state: shows unmatched warning when `matchedMbids.length < totalTracks`

### 6. `src/pages/PlaylistsPage/components/playlist-viewer/LbExportButton.tsx` — 48% stmts

The append and replace flows after confirming are untested. Add to `tests/components/playlist-viewer/LbExportButton.test.tsx`:

- append flow: pick existing playlist → click Append → click Confirm → calls `appendLbPlaylistTracks` → done state with link
- replace flow: pick playlist → click Replace → fetches `fetchLbPlaylistTracks` → shows replace-confirm; click backup → marks downloaded; click Replace → calls `replaceLbPlaylistTracks` → done state
- error in replace fetch: `fetchLbPlaylistTracks` throws → shows error state
- create-form: privacy toggle changes between Private/Public; Public flag passed to `createLbPlaylist`
- idle state: shows unmatched warning when `matchedMbids.length < totalTracks`

### 7. `src/pages/PlaylistsPage/components/playlist-viewer/PlaylistViewer.tsx` — 60% stmts

The `handleSetOverride` and `handleClearOverride` functions (lines 47–64) are never called in tests because `TrackTable` is mocked. To cover them, either:

- Don't mock `TrackTable` and instead mock one level deeper (mock `TrackTableRow`), then trigger an override/clear via a `MbBadge` or `UnresolvedCell` interaction
- Or test via the mock: assert that the `onSetOverride`/`onClearOverride` props passed to the mocked `TrackTable` actually call `navigate` with the right search params when invoked directly

The second approach is simpler: in a test, capture the props passed to the mocked `TrackTable`, call `onSetOverride("jf-id", "mb-id")` directly, and assert `mockNavigate` was called.

### 8. `src/pages/PlaylistsPage/components/playlist-selection/PlaylistSelection.tsx` — 68% stmts

Missing: error state and pagination next/prev clicks. Add to `tests/components/playlist-selection/PlaylistSelection.test.tsx`:

- shows error message when `fetchPlaylists` rejects
- next button advances to page 2 (shows remaining items); prev button goes back

## `rise-in` and `animate-slide-in-right` — register as Tailwind animate tokens

These are custom keyframe animations. They work fine as CSS classes, but Tailwind 4 lets you register them as theme tokens so they can be used as `animate-rise-in` alongside built-ins. Low priority — the current classes are clear enough.

## GitHub Pages deployment

SPA mode is configured (`spa: { enabled: true }` in vite.config.ts, outputs `dist/client/`). Need to wire up:
- GitHub Actions workflow to build and deploy to `gh-pages` branch
- `_redirects` / `404.html` fallback so all routes serve `/_shell.html`
- `base` path config if hosted at a subpath (e.g. `/<repo-name>/`)

## Refactor: file structure after inline row expansion

The inline row expansion PR converted match editing from popovers on badge buttons into inline expanded rows. This left some files with names and structure that no longer match their responsibility.

### 1. Rename and split `MbBadge.tsx`

`MbBadge.tsx` exports four components, but the file name only describes `MbBadge` — the 20-line dot-badge icon. The other three (`MbBadgeStatus`, `MbBadgeManualEntry`, `MbBadgeEditContent`) are expanded-panel UI, not badge-related. They were named during the popover era; that context no longer exists.

Split into two files:

- **`MbBadge.tsx`** — keep only `MbBadge` (the icon with colored dot).
- **`MatchEditPanel.tsx`** (new) — move and rename the three panel components:
  - `MbBadgeStatus` → `RecordingMatchStatus`
  - `MbBadgeManualEntry` → `ManualMbidEntry`
  - `MbBadgeEditContent` → `ConfirmedMatchPanel`

Update imports in `TrackTableRow.tsx` and test files.

### 2. Extract expanded row content from `TrackTableRow.tsx`

The unresolved/selected-override expanded content (lines 189–231) uses an IIFE to scope locals — a sign it should be its own component. Extract to **`CandidateSelectionPanel.tsx`**, accepting the relevant `matchState` slice plus `onSetOverride`, `onClearOverride`, `onCollapse` callbacks.

The partial-auto/confirmed-override branch (lines 165–187) can then be expressed as a direct `<ConfirmedMatchPanel .../>` once item 1 is done, reducing `TrackTableRow` to a coordinator that picks which panel to render.

### 3. Evaluate inlining `MbBadgeEditContent` / `ConfirmedMatchPanel`

After item 1, `ConfirmedMatchPanel` is a two-line composition of `RecordingMatchStatus` + `ManualMbidEntry` with one usage site. Decide whether it's worth keeping as a named export or just rendering the two components directly at the call site.

### 4. Consider barrel file or subdirectory structure for `playlist-viewer/`

The folder currently has 9 flat files; items 1–2 will add more. Options:

- Flat barrel (`index.ts`) re-exporting public components so siblings import from `"./index"`.
- Subdirectory: move `TrackTableRow` and its panel files into `track-row/`, keeping `PlaylistViewer`, `TrackTable`, `SyncDropdown`, `LbExportButton` at the top level.

Low priority — best done alongside items 1–2 to batch the import updates.

## MusicBrainz collections — track order

MB collections are unordered sets. The ListenBrainz export preserves order via JSPF playlists, but there's no equivalent for MB. Consider surfacing this distinction in the UI — perhaps a tooltip or note on the MB export button.

## Duplicate recordings in MB collections

MB collections are sets — duplicate MBIDs are collapsed. The export already warns the user when duplicates are present. Consider writing duplicate info into the MB collection description field (e.g. "Exported from Jellyfin playlist 'Road Trip'. Duplicates: 'I'm Gonna Be (500 Miles)' ×30"). Need to verify whether the collection API accepts a description field.
